# ТЗ: W-35 — «Законодательство» внутри кабинета + функции подписчика

Статус: реализовано (см. `web/app/routers/cabinet_zakon.py`, `services/cabinet_zakon.py`).

Публичный `/zakon` не меняется. Кабинет: `/cabinet/zakon/*` на тех же данных
(`legal_acts` / versions / fragments / `search_legal`).

Функции: закладки и история (всем залогиненным); заметки, слежение, DOCX —
тарифы Специалист/Организация. Письма по `law_watches` — только после
публикации редакции, дайджест ≤ 1 письмо/сутки на пользователя.
