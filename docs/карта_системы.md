# Карта системы Док.Москва — структура, связи, перелинковка, проблемы

Документ для последующего анализа архитектуры и UX/SEO-перелинковки.  
Актуален на состояние репозитория после W-00…W-33, W-19 (админка законодательства), bootstrap-наполнения текстов из ИПС, nav-order и ревизии прод-дыр.  
Источник правды по коду: `web/`, `core/`, `deploy/`, `docs/`.

---

## 0. Краткий вердикт

Один FastAPI-процесс обслуживает **два логических хоста**; разделение закреплено **host-middleware (W-26)**: публичные маршруты только на `dok.moscow`, кабинет/auth/admin/api/webhook — только на `app.dok.moscow` (чужой маршрут → 301; на app — `X-Robots-Tag: noindex`).  
Функционал кабинета **орг-центричен** (`org_id` + `FILES_ROOT/{org_id}`); права внутри org — **`users.org_role`** (`org_admin` / `org_member`, W-27).  
Стартовая точка — **дашборд `/cabinet/`** (W-28). Поиск единый (W-29): `services/search.py` → Cmd+K и `/cabinet/search`.  
Фон: **worker** (jobs PDF, биллинг, мониторинг НПА) — W-30; `/admin/status` — W-32; **ops-agent** + `/admin/server/` — W-34; хэшированная статика — W-31.  
Законодательство: `/zakon` + **`/admin/legal/`** (W-19): bootstrap ИПС → черновики; пакетная публикация кнопкой / CLI (не автомат мониторинга).  
Перелинковка app → лендинг/`/zakon` — футер кабинета и пункт «Законодательство». Заглушка `/cabinet/org/{id}` удалена (W-33).  
Остаточные разрывы: вычитка seed-текстов пленумов/приказов владельцем; `YANDEX_CLIENT_SECRET` / Hostland-даты — владельцу.  
W-35: кабинетное `/cabinet/zakon/*` (закладки, заметки⭐, цитата, слежение⭐, DOCX⭐, история); публичный `/zakon` без изменений.

---

## 1. Архитектура репозитория

```
desktop-tutorial /
├── web/                 # FastAPI + Jinja2/HTMX + Alembic + SQLAlchemy 2
│   ├── app/
│   │   ├── main.py      # create_app, middleware, include_router, static
│   │   ├── routers/     # HTTP-поверхность
│   │   ├── services/    # бизнес-логика
│   │   ├── templates/   # Jinja2
│   │   ├── static/      # CSS/JS/samples/favicons
│   │   ├── models.py, navigation.py, deps.py, security.py, …
│   │   └── billing/     # Т-Касса, crypto, jobs
│   └── alembic/
├── core/
│   ├── docfiller_core/  # генерация DOCX, комплекты, фильтры (без tkinter)
│   └── Шаблоны/         # DOCX + contracts_registry.json
├── deploy/              # compose, Caddy, backup, deploy.sh
├── docs/                # ТЗ, безопасность, эксплуатация, эта карта
└── scripts/             # setup_dev, make_samples
```

**Потоки данных**

```
Браузер
  ├─ dok.moscow  ─┐
  └─ app.dok.moscow ─┤→ Caddy → app:8000 (uvicorn)
                     │         ├─ PostgreSQL 16
                     │         ├─ Gotenberg (DOCX→PDF)
                     │         ├─ core/ (ro) + Шаблоны (RW для admin templates)
                     │         └─ FILES_ROOT=/srv/dok/files
                     └─ OUT: DaData, Т-Касса, SMTP, Yandex OAuth, pravo.gov.ru
```

| Пакет | Зависимость |
|--------|-------------|
| `web` → `core` | `CORE_PATH` / `sys.path`, шаблоны `TEMPLATES_DIR` |
| `deploy` → `web` | Docker image `web/Dockerfile`, entrypoint alembic+uvicorn |
| `scripts/make_samples` | core + Gotenberg → `web/app/static/samples/*.pdf` |

---

## 2. Топология хостов и деплоя

| Хост | Роль продукта | Фактически |
|------|----------------|------------|
| `dok.moscow`, `www.dok.moscow` | Лендинг, юр. страницы, `/zakon/`, SEO | Тот же `app:8000` |
| `app.dok.moscow` | `/login`, `/cabinet/*`, `/admin/*`, OAuth callback, webhook | Тот же `app:8000` |

Разделение **логическое** через `PUBLIC_BASE_URL` / `APP_BASE_URL` в шаблонах и письмах, не через разные сервисы.

**Compose:** `caddy` (:80/:443) → `app` (:8000) + `postgres` + `gotenberg` (fonts-liberation).  
**Секреты:** только `deploy/.env` (см. `.env.example`).

---

## 3. Слои доступа и изоляция

### 3.1. Роли

| Роль БД (`UserRole`) | Home | Доступ |
|----------------------|------|--------|
| `service_admin` | `/admin/` | Админка сервиса; `org_id` обычно `None` |
| `user` | `/cabinet/` | Кабинет своей организации |

В навигации (`NavRole`) есть `org_user` и `org_admin`, но **отдельного org_admin в БД нет** — всем пользователям с org выдаются обе роли (`navigation.py`). Пункт «Настройки» формально `org_admin`, фактически доступен всем в org.

### 3.2. Изоляция данных

- Запросы сущностей через `org_scope` / `get_*_for_org`.
- Файлы: `FILES_ROOT/{org_id}/…` и `…/templates/` для org DOCX.
- Сессия: `user_id`, `org_id`, `role`, `sid`, CSRF в session.
- Cookies: `dok_session`; опц. `dok_2fa_device`; опц. `SESSION_COOKIE_DOMAIN=.dok.moscow`.

### 3.3. Тарифные гейты

| Тариф | Суть |
|--------|------|
| `guest` | 3 документа/мес, водяной знак; платные разделы → upsell на `/cabinet/billing/` |
| `specialist` | Платный; party-check доступен |
| `organization` | Платный; + «Мои шаблоны», лимит пользователей (5) |

Бета: org без подписки → trial Organization до `BETA_TRIAL_UNTIL`.

---

## 4. HTTP-поверхность (карта URL)

### 4.1. Публичный слой (ожидается на `dok.moscow`)

| URL | Назначение | Шаблон / тип |
|-----|------------|--------------|
| `/` | Лендинг | `landing/index.html` |
| `POST /apply` | Заявка (honeypot + rate limit) | HTML |
| `/tariffs`, `/privacy`, `/offer`, `/requisites`, `/contacts` | Юр./маркетинг | `landing/*` |
| `/zakon/`, `/zakon/{slug}` | Каталог и карточка НПА | `zakon/*` |
| `/robots.txt`, `/sitemap.xml` | SEO | text/xml |
| `/static/*` | CSS/JS/PDF samples | StaticFiles |

### 4.2. Auth / OAuth (ожидается на `app.dok.moscow`)

| URL | Назначение |
|-----|------------|
| `/login`, `POST /login` | Пароль |
| `/login/2fa` | TOTP / backup |
| `/logout` | Выход |
| `/forgot-password`, `/reset-password/{token}` | Сброс |
| `/invite/{token}` | Принятие инвайта → session |
| `/auth/yandex/start` → oauth.yandex.ru | Старт OAuth + PKCE |
| `/auth/yandex/callback` | Завершение (login / register guest / link / 2FA) |

### 4.3. Кабинет `/cabinet/*`

| Префикс / путь | Модуль | Связанность |
|----------------|--------|-------------|
| `/cabinet/` → 303 `/cabinet/documents/` | entry | — |
| `/cabinet/documents/` | Документы | → PDF (Gotenberg), счётчики, лимиты, `/zakon/{slug}` из формы |
| `/cabinet/package/` | Мастер комплекта | → Document, Contract, Counterparty, calendar, zip/pdf |
| `/cabinet/templates/` | Org DOCX | тариф Organization; registry файловый |
| `/cabinet/counterparties/` | Картотека | DaData suggest HTMX; → package, party-check |
| `/cabinet/party-check/` | Проверка | DaData + PartyCheck; paywall; → save CP / package |
| `/cabinet/` | Дашборд (W-28): «Требуют внимания», счётчики, блок НПА | entry |
| `/cabinet/journal` | Журнал документов (+ HTMX rows) | → document, package/repeat |
| `/cabinet/search` | Полная страница единого поиска (W-29) | тот же движок, что Cmd+K |
| `/cabinet/calendar/` | События/напоминания | ← package done, counterparties |
| `/cabinet/billing/` | Тариф и оплата | → Т-Касса Init redirect; ← upsell |
| `/cabinet/settings/` | Реквизиты, банк, подписи, цены, счётчики, security (2FA, Яндекс, пароль) | org_admin / RO member |
| `/cabinet/staff/` | Сотрудники org (W-27) | только org_admin + тариф Organization |

### 4.4. API JSON `/api/*` (под сессией)

| URL | Назначение |
|-----|------------|
| `GET /api/global-search?q=` | Cmd/Ctrl+K: реестр + сущности org (+ admin) |
| `POST /api/nav-order` | Порядок пунктов сайдбара → `users.nav_order` |

### 4.5. Биллинг (тех.)

| URL | Назначение |
|-----|------------|
| `POST /billing/webhook` | Т-Касса → PlainText OK/ERROR |
| `/cabinet/billing/pay\|success\|fail` | UI оплаты |

### 4.6. Админка `/admin/*` (только `service_admin`)

Обзор · Организации · Пользователи (toggle, reset-2fa) · Заявки → invite · Приглашения · Шаблоны сервиса (`core/Шаблоны`) · **Законодательство** (`/admin/legal/` — diff, публикация, pull ИПС, bootstrap) · Платежи / сверка / refund · Платёжная система · Статус (конфиг + ops P50/P95, worker, бэкап) · **Сервер** (`/admin/server/` — ops-agent, бэкап, redeploy, Hostland).

---

## 5. Внутренняя перелинковка

### 5.1. Источник правды меню — `NAV_REGISTRY`

Файл: `web/app/navigation.py`.

- Меню кабинета/админки, глобальный поиск и sitemap-логика строятся из одного реестра.
- Поля: `key`, `title`, `url`, `synonyms`, `roles`, `tariffs`, `area`, `group`, `menu`, `quick_action`.
- Upsell: пункт виден guest’у, но `url` подменяется на `/cabinet/billing/`.
- Порядок: `POST /api/nav-order` + `nav-sortable.js` → JSON в `users.nav_order`.
- Техмаршруты вне меню — `ROUTE_EXCEPTIONS` (тест `test_registry_covers_app_routes`).

**Сайдбар кабинета (menu=True):**  
Документы → Комплект → Мои шаблоны* → Контрагенты → Проверка контрагента* → Журнал → Календарь → Тариф и оплата → Настройки  
(\* тарифный гейт)

**Сайдбар админки:**  
Обзор → Организации → Пользователи → Заявки → Приглашения → Шаблоны → Платежи → Платёжная система → Статус

### 5.2. Карта переходов пользовательского контура

```mermaid
flowchart LR
  subgraph public [dok.moscow]
    LP[Лендинг /]
    ZAK[/zakon/]
    LEGAL[privacy offer tariffs…]
    LP --> LEGAL
    LP --> ZAK
  end

  subgraph app [app.dok.moscow]
    LOGIN[/login]
    CAB[/cabinet/documents]
    PKG[package]
    CP[counterparties]
    PC[party-check]
    JRN[journal]
    CAL[calendar]
    BILL[billing]
    SET[settings]
    ADM[/admin/]
  end

  LP -->|app_base_url/login| LOGIN
  LP -->|app_base_url/cabinet/billing| BILL
  LOGIN --> CAB
  CAB <--> PKG
  CAB --> JRN
  PKG --> CP
  PKG --> CAL
  CP <--> PC
  PC --> BILL
  PC --> PKG
  JRN --> CAB
  JRN --> PKG
  CAB -.->|относительный /zakon| ZAK
  BILL -->|redirect| TBANK[Т-Касса]
  LOGIN --> YA[Yandex OAuth]
```

### 5.3. Связанный функционал (данные + UI)

| Контур | Связи |
|--------|--------|
| **Документы ↔ Комплект** | Общие шаблоны core, `allocate_number`, `Document`, лимиты генерации, watermark |
| **Комплект ↔ Контрагенты** | upsert CP из мастера; prefill из карточки; `package/repeat` |
| **Party-check ↔ Контрагенты ↔ Комплект** | save/update CP, session в package, журнал проверок |
| **Журнал ↔ Документы/Комплект** | список, download, repeat |
| **Комплект/CP ↔ Календарь** | автособытия сроков; ручные события с привязкой CP |
| **Всё генерационное ↔ Биллинг** | `assert_can_generate`, paywall party-check/templates, upsell в поиске |
| **Settings ↔ Auth** | 2FA, пароль, link/unlink Яндекс; политика require_2fa из PaymentSettings |
| **Leads ↔ Invites ↔ Users ↔ Orgs** | воронка админки |
| **Admin templates ↔ Documents/Package** | общий каталог `core/Шаблоны` |
| **Org templates ↔ Documents** | только тариф Organization, файлы org |

### 5.4. Изолированный / слабо связанный функционал

| Контур | Почему изолирован |
|--------|-------------------|
| **Законодательство `/zakon`** | Публичный контент; связь с кабинетом — редкая ссылка из `document_form`; мониторинг/поиск НПА фоном |
| **Webhook `/billing/webhook`** | Без UI-сессии; только Т-Касса |
| **Legal monitor / loaders** | Фоновые jobs + SMTP админу; нет кабинетного CRUD UI |
| **Глобальный поиск API** | UI-палитра; дублирует частично `/cabinet/search` |
| **Nav-order API** | Персонализация UI, не бизнес-данные |
| **Probe `/cabinet/org/{id}`** | Тестовая заглушка изоляции |
| **Статика samples** | Только лендинг; не связана с живыми документами пользователя |
| **Метрика** | Только лендинг (скрипт + CSP) |

### 5.5. Перелинковка в шаблонах (важное)

**Лендинг → app**

- `{{ app_base_url }}/login` — nav, hero, footer, 404, contacts, requisites  
- `{{ app_base_url }}/cabinet/billing/` — страница тарифов  
- якоря `/#apply`, `/#samples`; samples → `/static/samples/*.pdf`

**Лендинг ↔ себя**

- nav/footer: `/`, `/zakon/`, `/tariffs`, `/offer`, `/privacy`, `/requisites`, `/contacts`  
- sitemap/canonical через `public_base_url`

**App → лендинг**

- **Практически отсутствует** (нет «На сайт» в cabinet/admin/auth).  
- Из формы документа — относительный `/zakon/{slug}` (на origin app, не `public_base_url`).

**HTMX**

- `hx-boost` на body/sidebar; suggest: `hx-get` `/cabinet/counterparties/suggest/{party,address,bank}`  
- `hx-post` в шаблонах почти нет (обычные form POST).

---

## 6. Внешние связи и перелинковка

| Система | Направление | Где в продукте | Конфиг |
|---------|-------------|----------------|--------|
| **Яндекс OAuth** | OUT authorize/token/info; IN callback | `/auth/yandex/*`, settings security | `YANDEX_CLIENT_*`, `YANDEX_REDIRECT_URI` → **должен быть app-хост** |
| **Яндекс Метрика** | Браузер → mc.yandex.ru | Лендинг | `YANDEX_METRIKA_ID` + CSP |
| **DaData** | OUT HTTPS suggestions/findById | counterparties, party-check | `DADATA_KEY`, daily limit |
| **Gotenberg** | OUT Docker HTTP | PDF документов/комплектов/карточек | `GOTENBERG_URL` |
| **Т-Касса** | OUT Init/GetState/Charge; IN webhook | billing UI + `/billing/webhook` | ключи в `PaymentSettings` (Fernet) |
| **SMTP** | OUT | сброс пароля, лиды, биллинг/календарь/НПА письма | `SMTP_*`, `ADMIN_NOTIFY_EMAIL` |
| **publication.pravo.gov.ru** | OUT (часто HTTP) | мониторинг НПА | `PRAVO_PUBLICATION_BASE` |
| **ИПС pravo.gov.ru** | OUT | загрузка текста акта | `PRAVO_IPS_BASE` |
| **Google Fonts** | Браузер | landing + app CSS | CSP allowlist |
| **mailto** | Внешние | `hello@`, `privacy@`, `post@use.moscow` на LP | шаблоны |

**SEO внешнее:** `robots.txt` Allow `/`, Disallow `/cabinet`, `/admin`, `/invite`, `/apply`, `/login`; Sitemap на `dok.moscow`.

**CSP (`main.py`):** `default-src 'self'`; fonts/styles Google; scripts Метрика; `form-action 'self'` (оплата только через свой POST + redirect).

---

## 7. Модель данных (org-centric sketch)

```
organizations ─┬─ users (org_id nullable)
               ├─ invites
               ├─ counterparties ─┬─ contracts ── documents
               │                  ├─ party_checks
               │                  └─ calendar_events
               ├─ documents
               ├─ counters (PK org_id+key)
               ├─ subscriptions → tariffs
               ├─ payments
               ├─ calendar_events
               └─ events (audit)

users ─┬─ oauth_identities
       └─ password_reset_tokens
       └─ nav_order (JSON)

leads, payment_settings (singleton)

legal_acts ─┬─ act_versions / act_fragments
            ├─ legal_search_docs
            └─ act_watch_log
```

Файлы вне БД: `FILES_ROOT/{org_id}/…`, org templates + registry; сервисные шаблоны в `core/Шаблоны/`.

---

## 8. Сессии, безопасность, кросс-хост

| Механизм | Поведение |
|----------|-----------|
| Session | SameSite=Lax; `SESSION_HTTPS_ONLY` на проде |
| CSRF | Signed token **в сессии** + form / `X-CSRF-Token` (отдельной CSRF-cookie нет, T3) |
| 2FA | TOTP + backup; device cookie; admin reset; политика для org |
| OAuth state | Signed timed payload с PKCE verifier (не зависит от cookie хоста лендинга) |
| Rate limit | In-memory (login, 2FA, reset, leads, webhook) — не шарится между репликами |
| Org isolation | deps + org_scope + probe route |

**Критичный ops-контракт:** Redirect URI Яндекса и cookie сессии должны жить на **`app.dok.moscow`**, не на лендинге.

---

## 9. Шаблоны и статика (инвентарь)

~62 HTML в `web/app/templates/`:

- `landing/*` + `zakon/*` — публичные  
- `auth/*` — вход/инвайт/сброс/2FA  
- `cabinet/*` + partials suggest — кабинет  
- `admin/*` — админка  
- `base.html`, `partials/global_search.html` — shell + Cmd+K  

Статика: `app.css`, `landing.css`, `htmx*.js`, `global-search.js`, `nav-sortable.js`, `favicon.*`, `samples/{dogovor-fl,schet,akt}.pdf`.

Заглушка: `cabinet/placeholder.html` (только probe org).

---

## 10. Проблемы и долги (для анализа)

### 10.1. Продукт / UX / перелинковка

| # | Проблема | Статус | Комментарий |
|---|----------|--------|-------------|
| P1 | Нет ссылок **app → лендинг** | **Закрыто W-26** | Футер кабинета + «Законодательство» на public |
| P2 | `/zakon` на **app-origin** | **Закрыто W-26** | Host-middleware + абсолютные ссылки |
| P3 | Два поиска | **Закрыто W-29** | Общий `services/search.py` |
| P4 | `org_admin` только в nav | **Закрыто W-27** | `users.org_role` |
| P5 | Upsell подменяет URL меню | Смягчено | Badge тарифа в сайдбаре; URL → биллинг |
| P6 | Placeholder `/cabinet/org/{id}` | **Закрыто W-33** | Маршрут удалён |
| P7 | ASVS V12.1 vs org templates | **Закрыто PART** | OOXML/zip + `SandboxedEnvironment` в `filler.py`; AV нет |

### 10.2. Технические / ops

| # | Проблема | Статус | Влияние |
|---|----------|--------|---------|
| T1 | `create_all` + Alembic | **Закрыто** | `create_all` только при `DB_AUTO_CREATE`; прод — Alembic в entrypoint |
| T2 | In-memory rate limits | **Закрыто** | `rate_limit_hits` в БД; bucket на лимитер |
| T3 | Мёртвый `csrf_cookie` в Settings | **Закрыто** | Поле удалено; CSRF только session-token |
| T4 | OAuth URI / `SESSION_HTTPS_ONLY` вручную на VDS | **Частично** | На VDS: `SESSION_HTTPS_ONLY` + redirect URI; `YANDEX_CLIENT_SECRET` — задать владельцу |
| T5 | Dual-host один процесс | Частично W-26 | Логическая граница есть |
| T6 | pravo.gov.ru часто по HTTP | Открыто | Осознанный компромисс |
| T7 | SQLite lock счётчиков | Открыто | Хрупко в dev/тестах |
| T8 | Desktop `autocomplete` / `linked` | **Частично: form-assist v1++** | UI: `data-linked` + `current` в API/JS; DaData bank; должность→род; дальше по жалобам |
| T9 | OpenAPI отключён | Открыто | Норма для MVP |
| T10 | ПДн: IP в `events` | **Закрыто PART** | `mask_ip` в `record_event` (IPv4 /24, IPv6 /64) |

Снято смежными пакетами: синхронный PDF → jobs/worker (W-30); статика → hash/Caddy (W-31); фон/вебхук → `/admin/status` (W-32); админка НПА + bootstrap ИПС (W-19 / fill).

### 10.3. Уже закрытые классы дефектов

- Yandex callback / PKCE; `SESSION_HTTPS_ONLY`; cookie только на app (W-26).  
- Gotenberg в `deploy.sh`; `ROUTE_EXCEPTION_NOTES` (W-33).  
- SEO dual-host, двойной поиск, фиктивный org_admin, `/cabinet/org` (W-26…W-33).  
- Публикация НПА без UI → `/admin/legal/` + bootstrap черновиков из ИПС.

### 10.4. Рекомендуемые оси анализа (следующие шаги)

1. ~~Схема БД / create_all~~ — T1: только `DB_AUTO_CREATE` или тесты; прод — Alembic.  
2. ~~Desktop parity autocomplete/linked~~ — form-assist UI+API wired; дальше — по жалобам.  
3. ~~Rate limit multi-replica~~ — T2: `rate_limit_hits` (Postgres/SQLite), без Redis.  
4. ~~Мёртвый `csrf_cookie`~~ — T3: поле удалено; CSRF = session + form / `X-CSRF-Token`.  
5. ~~ASVS / org templates~~ — P7: V12.1 PART + `docx_upload` проверки.  
6. ~~НПА ↔ генерация~~ — манифест `web/app/data/template_normative.json` (by_name + substring).  
7. ~~SEO sitemap~~ — только акты с published (или card) + `lastmod`.  
8. ~~Пакетная публикация черновиков~~ — `/admin/legal/publish-drafts` + `scripts/publish_legal_drafts.py`.  
9. **Вычитка владельцем** — пленумы/приказы без IPS; `YANDEX_CLIENT_SECRET` в `deploy/.env`.  
10. ~~Jinja sandbox~~ — `SandboxedEnvironment` в `fill_template` / `list_template_variables`.  
11. **IPS fulltext** — `doc_itself&fulltext=1` (иначе кодексы обрезаются).


## 11. Указатель ключевых файлов

| Тема | Путь |
|------|------|
| Entry / CSP / routers | `web/app/main.py` |
| Конфиг / URL хостов | `web/app/config.py` |
| Реестр навигации | `web/app/navigation.py` |
| Form-assist (T8) | `web/app/services/form_assist.py`, `routers/form_assist.py` |
| Rate limit (T2) | `web/app/rate_limit.py`, таблица `rate_limit_hits` |
| Загрузка DOCX (P7) | `web/app/services/docx_upload.py` |
| Нормативная база шаблонов | `web/app/data/template_normative.json` |
| Модели | `web/app/models.py` |
| Изоляция org | `web/app/org_scope.py`, `deps.py` |
| OAuth | `web/app/yandex_oauth.py`, `routers/yandex_auth.py` |
| 2FA | `web/app/totp_2fa.py` |
| Биллинг | `web/app/services/billing.py`, `web/app/billing/` |
| Деплой | `deploy/compose.yml`, `Caddyfile`, `deploy.sh` |
| Безопасность | `docs/безопасность.md` |
| Эксплуатация | `docs/эксплуатация.md` |
| ТЗ | `docs/ТЗ_веб_MVP.md`, `ТЗ_биллинг.md`, `ТЗ_законодательство.md`, `ТЗ_улучшения.md`, `ТЗ_пакеты_W20W34.md`, `ТЗ_W35_кабинет_законодательство.md` |
| Сервер из админки | `docs/сервер_из_админки.md` (W-34) |
| Кабинетное законодательство | `web/app/routers/cabinet_zakon.py`, `services/cabinet_zakon.py` (W-35) |

---

*Конец карты. При существенных изменениях маршрутов или тарифов обновлять §§4–6 и §10.*
